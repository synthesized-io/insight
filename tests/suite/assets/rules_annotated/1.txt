CASE
	WHEN KRO.STATUS = 'LNLP' THEN 'ACTIVE' AS VARCHAR(100)
	WHEN [KRO].[STATUS] IN ('HOMES','MIDAS') THEN 'ACTIVE(2)' AS NVARCHAR(250)
	WHEN LEN(KRO.STATUS) IS NULL THEN NULL
	ELSE 'ENDED' AS VARCHAR(100)
END
